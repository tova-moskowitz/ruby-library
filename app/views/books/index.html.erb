<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>All Books</title>
  </head>
  <body>
    <ul>
      <% @books.each do |book| %>
    <li><%= book.title %>
<ul>
  <li><%= book.description %></li>
  <li>$<%= book.price %></li>
  <li><%= book.subject.name %></li>
</ul>

    </li>
    <% end %>
    </ul>
  </body>
</html>
